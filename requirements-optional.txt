# Heavy runtime stack for the vectorized/scientific simulation engine
# (scrubin/runtime/vectorized, scrubin/learning, scrubin/validation, ...).
#
# NOT required by the CI gates or the pytest suite -- every module that
# uses these lazy-imports them behind try/except and degrades gracefully.
# Install on demand with the CPU-only torch build from the PyTorch index:
#
#   pip install --extra-index-url https://download.pytorch.org/whl/cpu -r requirements-optional.txt
#
torch==2.13.0+cpu
numpy==2.5.1
gymnasium==1.3.0
dipy==1.12.1
matplotlib==3.11.1
plotly==6.9.0
scikit-learn==1.9.0
scipy==1.18.0
pandas==3.0.5
nibabel==5.4.2
SALib==1.5.2
h5py==3.16.0
trx-python==0.4.0
